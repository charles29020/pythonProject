# 37_what_is_machine_learning.py

# install scipy -> pip install scipy
# scipy also installs numpy
# also, install scikit-learn -> pip install scikit-learn

# we will switch to jupyter, to do this, we first must install ipython -> pip install ipython
# next we will install the jupyter notebook -> pip install "ipython[notebook]"
# to run the jupyter notebook, from the terminal, type in -> jupyter notebook

# will open in web browser (chrome) inside a folder (.../Learning/PyCharm/Projects/xyz)

# create a new file by clicking in the top right corner the [new] Python 3 button

# a notebook will be created where code can be completed in segments


